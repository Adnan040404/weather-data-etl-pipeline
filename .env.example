# Copy this file to .env and fill it in. .env is ignored by git and must never be committed.
# Free key: https://openweathermap.org/api (sign up, then API keys)
OWM_API_KEY=your_openweathermap_key_here
